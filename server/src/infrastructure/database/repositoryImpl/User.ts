import {UserRepository} from "../../../core/repository/UserRepository/UserRepository.js";
import {CreateUserDTO} from "../../../core/repository/UserRepository/dto/CreateUserDTO.js";
import {User} from "../../../core/entities/User.js";

/*
export class UserRepositoryImpl implements  UserRepository {
    create(dto: CreateUserDTO): User {

        const user = sequelize.add(dto)
        return user;
    }

    delete(id: number): string {
        return "";
    }

    get(id: string): User {
        return undefined;
    }

    update(id: number): User {
        return undefined;
    }

}
*/

